/**
 * Lesson → vocab ids for practice gating (starter + HSK 1).
 * Kept separate from full course-content to avoid pulling HSK 2–6 bundles into client pages.
 */
export { lessonVocabMap } from "@/data/starter-hsk1/lesson-maps";
